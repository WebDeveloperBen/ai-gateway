// Schema definition - defines the public schema
schema "public" {
  comment = "standard public schema"
}

// Note: PostgreSQL extensions, functions and triggers are managed through
// separate SQL files due to Atlas free tier limitations.